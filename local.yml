- hosts: localhost
  connection: local
  become: true

  pre-tasks:
    - name: update repositories
      apt:
        update_chache: yes
      changed_when: False

  tasks:
    - include: tasks/packages.yml
